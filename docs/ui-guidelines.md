# UI Guidelines

The Task Manager application should follow these core UI guidelines:

- Use Material components.
- Color palettes should be dark green.
- Button styles should match the color palette and be slightly rounded.
- Include two UI configuration buttons:
  - One button to make text twice as large.
  - One button to switch to a lighter color palette.
