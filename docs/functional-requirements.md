# Functional Requirements

The Task Manager application must support the following core functional requirements:

- Users can create tasks.
- A user can add a due date to a task.
- Existing tasks can be edited.
- Tasks can be sorted either alphabetically or by due date. When due dates are the same, those tasks should be sorted alphabetically.
- Users can delete tasks.
